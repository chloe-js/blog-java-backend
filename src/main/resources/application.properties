# linking springboot with mySQL database === open xampp and open Apache and MySQL == create New = blogPosts && ! RESOURCES > application.properties
# Configuration
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/blogPosts
spring.datasource.username=root
spring.datasource.password=

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver